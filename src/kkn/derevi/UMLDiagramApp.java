package kkn.derevi;

public class UMLDiagramApp {

    public static void main(String[] args){

        //uncommment block once UI completed so that user can input parameter to choose proper UI

/*        if(args.length==0){
            System.out.println("Usage: java UMLDiagramApp <option>");
            System.err.println("An UI option is required");
            System.exit(1);
        }

        while (args[0].equals("-t")) {

        }*/

        //Todo rename Mains, to app
        //Todo use args to initiate app with UI choice
        //Todo Create text based UI, as this will be a terminal app, ensure future gui implementation for app is simple.

    }
}
